stream 모듈의 기존 `index.html`을 분석한 뒤, 기능은 그대로 유지하고 UI만 상용 챗봇 수준으로 개선한 `chatbot.html`과 `chatbot.css`를 만들어 주세요.

기존의 메시지 전송, SSE 스트리밍 응답 처리, 응답 중지 기능은 그대로 유지하고, 기존 `index.html`의 요청 방식 및 이벤트 처리 로직은 변경하지 마세요.

스타일은 최대한 `chatbot.css`로 분리하고, HTML 내부에는 inline style 사용을 최소화해 주세요.

UI는 Tailwind CSS CDN을 사용하여 실제 ChatGPT, Claude와 유사한 현대적인 채팅 인터페이스로 구성해 주세요.

아이콘은 Lucide Icons CDN을 사용해 주세요.

전송 버튼과 중지 버튼은 하나의 버튼으로 통합해 주세요.

* 대기 상태 → lucide-send 아이콘 표시
* 응답 생성 중 → lucide-square 아이콘 표시

채팅 화면 요구사항은 다음과 같습니다.

* 전체 화면 높이를 사용하는 채팅 레이아웃
* 중앙 정렬된 최대 너비 컨테이너
* 상용 서비스 수준의 깔끔한 디자인
* 사용자 메시지는 오른쪽 정렬
* AI 메시지는 왼쪽 정렬
* 사용자 메시지 버블은 파스텔 톤의 파란색 계열
* AI 메시지 버블은 연한 회색 계열
* 둥근 모서리와 적절한 여백 적용
* 긴 메시지는 자동 줄바꿈
* 입력 영역은 화면 하단에 고정
* EventSource 연결 상태 표시 영역은 제거

최종 결과로 다음 파일 전체 코드를 제공해 주세요.

* chatbot.html
* chatbot.css

기존 `index.html`을 대체하여 바로 실행 가능한 형태로 작성해 주세요.
